"""Persistent homology computation on exploration trajectories.

Computes Vietoris-Rips complexes at multiple scales on trajectory
point clouds, tracking H_0 (components), H_1 (loops), H_2 (voids).

Uses giotto-tda or ripser as backend.

Implementation: Phase 3
"""
